# ========================================
# Local Development Environment Variables
# ========================================
# 
# INSTRUCTIONS:
# 1. Copy this file: cp .env.dev.local.example .env.dev.local
# 2. Update values as needed for your local testing
# 3. NEVER commit .env.dev.local to git (it's already in .gitignore)
#
# NOTE: These values are for LOCAL TESTING ONLY
# Production/Staging values are managed via AWS SSM/Secrets Manager

# ========================================
# AWS Configuration (Optional Overrides)
# ========================================
# AWS_REGION=ap-south-1
# AWS_PROFILE=myapp-dev

# ========================================
# Local Testing Resources
# ========================================
# Use these IDs for local testing (they point to dev environment resources)

# Cognito User Pool (Public ID - safe to include)
COGNITO_USER_POOL_ID=ap-south-1_De9eHp3Wi

# S3 Bucket for local testing
S3_APPLICATION_BUCKET=myapp-assets-local-testing

# Email sender (for SES testing)
FROM_EMAIL=noreply@dev.fitnearn.com

# ========================================
# SSM Parameter Store Paths
# ========================================
# These paths tell Lambda where to fetch secrets from AWS
# DO NOT put actual secret values here!

COGNITO_CLIENT_ID_PATH=/myapp/dev/auth/clientId
COGNITO_CLIENT_SECRET_PATH=/myapp/dev/auth/clientSecret
MONGODB_SECRET_ID=myapp/dev/mongodb

# ========================================
# Local Development Features
# ========================================
# NODE_ENV=development
# LOG_LEVEL=debug

# ========================================
# NEVER INCLUDE THESE IN THIS FILE:
# ========================================
# ❌ AWS Access Keys (use AWS CLI profiles instead)
# ❌ MongoDB connection strings (use Secrets Manager)
# ❌ Cognito client secrets (use Parameter Store)
# ❌ API keys or passwords
# ❌ Any production credentials
